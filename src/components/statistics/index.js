import statisticsComponent from './statistics.component';

const statisticsModule = angular.module('app.statistics', []);
statisticsModule.component('statistics', statisticsComponent);

export default statisticsModule;
